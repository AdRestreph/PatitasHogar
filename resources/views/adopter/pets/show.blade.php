@extends('layouts.app')

@section('content')
    <a href="{{ route('adopter.pets') }}" class="text-secondary text-decoration-none">← Volver al catálogo</a>
    <div class="row g-4 mt-1"><div class="col-lg-5"><div class="surface h-100">@if($pet->photo)<img src="{{ asset('storage/'.$pet->photo) }}" class="img-fluid w-100 rounded-4 pet-image" alt="{{ $pet->name }}">@else<div class="pet-placeholder rounded-4"><i class="bi bi-heart-fill"></i></div>@endif<h1 class="mt-4 mb-1">{{ $pet->name }}</h1><span class="badge badge-status-available">{{ ucfirst($pet->status) }}</span><p class="mt-3 mb-0 text-muted">{{ $pet->description }}</p><hr><div class="row text-center"><div class="col-4"><strong>{{ $pet->species->name }}</strong><small class="d-block text-muted">Especie</small></div><div class="col-4"><strong>{{ $pet->age_months }} meses</strong><small class="d-block text-muted">Edad</small></div><div class="col-4"><strong>{{ ucfirst($pet->size) }}</strong><small class="d-block text-muted">Tamaño</small></div></div></div></div><div class="col-lg-7"><div class="surface"><h2>Solicitar adopción</h2><p class="text-muted">Cuéntanos por qué {{ $pet->name }} sería feliz contigo.</p><form method="POST" action="{{ route('adoption.store', $pet) }}" class="row g-3">@csrf<div class="col-12"><label class="form-label" for="reason">¿Por qué quieres adoptar?</label><textarea id="reason" name="reason" rows="4" class="form-control" required>{{ old('reason') }}</textarea></div><div class="col-md-7"><label class="form-label" for="home_type">Tipo de vivienda</label><input id="home_type" type="text" name="home_type" class="form-control" value="{{ old('home_type') }}" placeholder="Ej. Apartamento" required></div><div class="col-md-5"><label class="form-label" for="has_other_pets">¿Tienes mascotas?</label><select id="has_other_pets" name="has_other_pets" class="form-select"><option value="0">No</option><option value="1">Sí</option></select></div><div class="col-12"><button type="submit" class="btn btn-primary">Enviar solicitud <i class="bi bi-send ms-1"></i></button></div></form></div></div></div>
@endsection
